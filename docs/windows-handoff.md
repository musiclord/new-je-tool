# Windows 手動驗證清單

這是一份滾動式交接清單，只列「必須在實際 app（WinForms／WebView2）裡親手點過才能確認」的項目。自動化測試驅動不了 GUI，所以這些項目只能靠人工目視。自動化測試（`dotnet test`）由 agent 自跑，目前全綠。

新增任務卡時請寫成自含的一張卡，四個欄位缺一不可：啟動指令、涵蓋內容、預期結果、失敗回報格式。失敗回報要寫清楚三件事——在哪一步、做了什麼、看到什麼；若 F12 console 有錯，一併附上。共通的啟動指令是 `dotnet run --project src/JET/JET`（Debug 組建）；多數項目可先用「套用測試案件」按鈕備妥資料。某張卡驗收通過後，把它移到下方「已完成」段。

## 待驗收

- **Step 4 進階條件篩選 UX（整併 2026-06-23 r2 至 2026-06-24 r12；以下為「目前應呈現的狀態」，不再分版本）**
  - 設計提案散見 `docs/specs/2026-06-23-*` 與 `docs/specs/2026-06-24-*`。啟動：`dotnet run --project src/JET/JET`（Debug）；先「套用測試案件」備資料。純前端，`dotnet build` 已綠（0 警告 0 錯誤）。送後端的情境只含名稱/動機/條件群組（`__kctLetter`、`__kctPresetGroup`、`__active` 等 UI-only 標記不外洩）。逐項目視：
  - **A. KCT條件 面板（獨立、可複選）**
    1. Step 4 頂部有獨立顯著的「KCT條件」面板，A–J 十顆卡（中文標籤），與下方自訂條件明顯區隔。**B** 為 disabled 標「Phase 2 · 待 BS/IS」；科目配對未匯入時 **A/C/D** 為 disabled 標「需先匯入科目配對」。
    2. KCT 卡**跟隨「作用中」組的 toggle**（r10）：已選（＝該訊號在**作用中組**裡）＝左側字母框反白＋整卡淡藍底，再點取消（**只從作用中組移除、別組同訊號不動**）；某訊號用於**別組**但作用中組沒有時，卡片顯示**藍邊＋小字「也在其他組」**（藍邊無底）；切換作用中組，面板高亮跟著反映那組。版面**不位移**。**情境名稱**：單組 `E+F+G`（依 A→J）；**多組以 `｜` 分組**（如 `G+H｜J`，同訊號跨組 `G+H｜G+J`，預設(I) 為**自己的 token** 如 `G｜H｜I`、`G｜I`，只有自訂條件的組顯「自訂」）；row〔移除〕／改型別／〔移除這組〕後名稱**即時重算**（只在涉及 KCT 字母時重算，不誤清純自訂的手改名）；動機逐行列說明，兩欄可手改、全取消即清空。**非營業日(I)** 為**情境層級**獨立條件（r12 特例：週末 OR 假日、結構上巢狀 OR，2-level 模型只能自成一組）：卡片用**黃色「情境層級」標記**（**非**組層藍色高亮），整組加/移、不顯「也在其他組」；**不論作用中組為何皆顯示已加入**（黃標），故「第 3 組空、作用中時 I 仍顯示已加入」是**正常**（I 屬情境層級、與當前組無關）。
       - **關鍵驗收（解 r9→r10 衝突）**：選 G+H（第 1 組）→〔＋另一組條件〕（第 2 組作用中）→ 上方 G/H 顯「也在其他組」；**在第 2 組點 G ＝加到第 2 組、第 1 組 G 不被刪**（名稱變 `G+H｜G+J`）。點第 1 組設為作用中 → G/H 亮、J 顯「也在其他組」；點 G ＝只從第 1 組移除。
    3. KCT 卡與自訂卡**等高對齊**、長標籤最多兩行（超出 … 截斷、hover 顯示全文）；停用卡與啟用卡等高、不撐高頂排。
  - **B. 自訂篩選條件（可折疊、已精簡）**
    4. 區塊**預設收起**（只剩「▸ 自訂篩選條件」標頭），點開才顯示四分類卡片；卡只有「＋」與名稱（無 ×N 計數），點一張即新增一條可重複條件，**加到下方「作用中」的組**（見 C-10）。展開後**整體已精簡**（卡片變矮、單行標籤、欄較窄較密、說明句縮短）——比舊版小一圈；KCT 面板不變。
    5. 含「考量特殊科目類別配對」條件（需先匯入科目配對）：三下拉——模式（Dr A、Cr B／Dr A、Cr 非 B／Dr 非 A、Cr B）、借方類別(A)、貸方類別(B)；摘要讀作「特殊科目配對：借…／貸…（模式）」；與「科目配對分析」可互切、重建無殘留。
  - **C. 建立篩選情境（單一介面、AND/OR 段控、父子層級對比、作用中群組目標、無模式）**
    6. **一個或多個「條件組」**：選 G+H → 一塊**淡色 well**，頂部一條「**條件之間** ‹AND│OR›」**灰階小段控**（mono；hover 有白話 tooltip「每個條件都要成立／符合任一個就好」），下接條件清單；一組時**看不到「組」字、無連接器**（最乾淨）。選 **G+I+J**：G、J 在 well，**非營業日(I) 改呈所有組下方的獨立「情境層級」黃色區塊**（pale-yellow 標籤＋「套用到整個情境（與上方條件 AND）」），**不再併入 well**（r12）。**沒有「進階：分組」模式字樣**。
    7. **教學空狀態**：尚無條件時，彙整區顯示「**還沒有任何條件。到上面挑一張『KCT條件』卡，或展開『自訂篩選條件』…**」（指向上方 palette），而非空白或「尚未選取」。
    8. **行內布林 read-back**：條件下方常駐一句「**這個情境會找出符合下列邏輯的分錄：**」＋布林式（單組例：`摘要空白 AND 金額尾數 000000`，AND 為灰色 mono），隨條件/段控**即時更新**；可直接當底稿的條件邏輯。**值編輯也即時**：在金額/日期/關鍵字等輸入框打字，提示**邊打邊同步**（不必等新增條件才更新），且打字時焦點不被吃。含**非營業日(I)** 時末端為「**（…） AND 非營業日（週末或假日）**」（情境層級、AND 到整體）；**剛按〔＋另一組條件〕的空組不進 read-back**（無懸空運算子如 `… OR AND …`，r12 修）。
    9. **＋ 另一組條件 與父子層級對比**：點〔**＋ 另一組條件**〕→ 出現第 2 塊 well（標題「**第 1 組／第 2 組**」，各自一個**灰階**小段控）；兩組之間是一條**水平軌道**、置中一個**藍色較大**段控「**組間** «AND│OR»」（預設 OR）。**藍色＝情境層 vs 灰色＝組內**，父子對比明顯；read-back 呈「**（… AND …） OR （… AND …）**」（OR 藍粗、AND 灰）。**無整句 radio／pill**。新增的第 2 組**自動成為「作用中」**（見下）。
    10. **作用中（active）群組目標＋非作用中鎖定＋回得去**：≥2 組時，**點任一組設為「作用中」**——作用中組有**左側藍軸＋淡藍底＋「作用中」徽章**；**其他組鎖定**（條件清單與組合器**變淡且點不動**，r11）、顯示小灰字「**點此設為作用中**」、hover 邊框轉強；**點鎖定組任一處即設為作用中**（控制項不可互動，點擊穿透到整塊 well）。**上方面板（KCT條件／自訂篩選條件）新增的條件，一律加到「作用中」那組**（解「分出第 2 組後回不去設定第 1 組」——點第 1 組設為作用中即可往它加條件）。**只有作用中組可編輯**；在作用中組改值不奪焦。≥2 組時情境區頂部有**全域提示**。**單組時不顯示作用中/鎖定樣式**（最乾淨）。每組有〔**移除這組**〕（鎖定組也可直接移除），移除作用中組會**回退**到其他組、移到只剩一組**自動回乾淨單 well**；建到一半的**空組不會讓預覽報錯**（送出時自動略過）。
       - **關鍵驗收（r11）**：反覆切換第 1/2 組並取消 KCT 不再錯亂——非作用中組點不動其下拉/輸入（只會被設為作用中）；用 row〔移除〕刪掉某組的 KCT 條件後，名稱與結構**即時一致**；`G+I｜G+J` 命名；純自訂情境手改名不被清空。
    11. **沿用前版＋修正**：每組組合器只在該組 ≥2 條件時顯示；移除非營業日同步取消 KCT 的 I 卡；動機欄 4 列不截斷；最上方一行 4 拍提示「挑訊號 → 設定數值 → 組合 → 命名保存」。**#1 修正**：「特定金額尾數」列的 input 與型別下拉、〔移除〕**同列置中對齊**，「例：999999 或 000000」乾淨落在 input 下方、**不再頂歪輸入框、不壓下一列**。
       - **關鍵驗收（r12）**：分出第 3 組（空、作用中）時——上方 **I 卡為黃色「情境層級」**（不是藍色已選）、G/J 顯「也在其他組」（這是正常，I 屬情境層級）；點 I → 所有組下方出現黃色「**情境層級｜非營業日（週末或假日）**」獨立區塊（不在任何「第 N 組」裡）；read-back 末端為「**） AND 非營業日（週末或假日）**」、**無 `OR AND` 懸空**；名稱含 `…｜I`。
  - **D. 預覽／保存／矩陣**
    12. 名稱與動機**必填**；〔預覽這個情境〕〔保存為篩選情境〕、已儲存清單詳情/移除、〔預覽測試母體〕、高風險條件矩陣均正常；切換組合器後命中數應隨布林邏輯改變。
  - **E. 同期（2026-06-23 r2）一併調整、非 Step 4 但仍待目視的項目**
    13. **命名**：視窗標題與頁首為「分錄測試自動化工具」（非舊「JET 傳票測試工具」）；「建立案件」無「產業別」欄、摘要無「金額 scale」列。
    14. **科目配對序位**：「匯入資料」無科目配對卡（GL/TB/授權編製人員/行事曆四卡仍在）；「資料驗證與測試」在驗證卡與預篩選卡之間有科目配對區塊，**執行驗證後**才出現〔選擇/重新匯入科目配對檔〕。
    15. **日期維度週幾**：Step 1 日期維度卡有一週七天勾選，預設勾週六日；改後即時保存、`project.load` 保留；影響週末過帳/核准與週末篩選。
    16. **資料預覽正規目錄**：資料集選單顯示正規審計名（JE_PBC／TB_PBC／JE／TB／ACCOUNT_MAPPING／AUTHORIZED_PREPARER／DATE_DIMENSION／資料庫結構總覽）；DATE_DIMENSION 顯示日期/類別/說明；結構總覽約 14 列、不含 staging/scratch/系統表、空專案開啟不報錯。**未實體改名任何資料表**。
  - 失敗回報格式：在哪一步、做了什麼、看到什麼（面板/卡片/扁平呈現/切換/折疊/必填/命中數/命名/解鎖時機/預覽名 異常、報錯）；F12 console 有錯一併附上。

- **匯出底稿存檔對話框與端到端匯出（子專案 E1，匯出底稿 writer；Task 6）**
  - 啟動：`dotnet run --project src/JET/JET`（Debug）。先在「匯入資料」用「套用測試案件」備妥 GL/TB，在「欄位配對」完成 GL/TB 配對，在「資料驗證與測試」按「執行驗證」，並在「進階條件篩選」保存至少一個會命中的情境，好讓 step3／step4 矩陣有資料。
  - 涵蓋內容：到「匯出底稿」步驟，按〔選擇位置並匯出底稿〕。
    1. 應彈出原生 SaveFileDialog（`host.selectSavePath`），filter 為「Excel 活頁簿 (*.xlsx)」，預填檔名為 `{公司名}_{yyyymmddHHmmss}_WorkingPaper.xlsx`（公司名取自當前專案，時間戳是當下時間）。
    2. 選一個路徑存檔後，後端會串流寫出（`export.workpaperStream`），面板顯示完成統計：存檔路徑、檔案大小（位元組）、各工作表的資料列數表。
    3. 取消 SaveFileDialog 時，訊息面板應顯示「已取消匯出底稿。」，不寫檔、不報錯。
  - 預期結果：
    - 預填檔名的時間戳是按下當下產生的（不是固定值）；取消對話框能安全中止。
    - 寫出的 `.xlsx` 用 Excel 開啟時不應出現修復提示。內容應含封面（公司名等於當前專案的客戶名）、step1 家族、step2 可靠性、step3／step4／step4-1 高風險矩陣（與已保存情境對得起來）、step5、以及三張「自動化工具」參考表。
    - 完成統計裡各表的列數要與實際工作表的資料列數相符。step3 的命中傳票數要與「進階條件篩選」裡該情境單獨預覽的結果對得起來，藉此證明匯出前已 materialize。
  - 失敗回報格式：在哪一步（上列 1–3）、做了什麼、看到什麼；F12 console 有錯一併附上。常見問題例如：對話框沒彈出、預填名或 filter 不符、寫出的檔 Excel 打不開、公司名錯、矩陣與情境對不起來、完成統計列數不符、postMessage 例外。

- **高風險條件矩陣預覽（子專案 D2，多情境逐列 tag 矩陣；Task 5）前端**
  - 啟動：`dotnet run --project src/JET/JET`（Debug）。先在「匯入資料」用「套用測試案件」備妥 GL/TB，在「欄位配對」完成 GL/TB 配對，再到「進階條件篩選」保存**至少兩個會命中的情境**（可先用「套用測試案件」備一個，再手動加幾個 prescreen 或自訂條件情境，最多到 10 個）。
  - 涵蓋內容：「進階條件篩選」頁底多了一張「高風險條件矩陣」卡。按〔展開矩陣〕會惰性載入三段。
    1. **情境摘要**（step3）：一列對一個已存情境，也就是一個高風險條件欄，顯示「欄（C1..CN）／條件名稱／命中傳票數／命中行數」。欄序依後端回的 position 升冪。
    2. **傳票矩陣**（step4）：每列一張命中傳票，有固定欄（傳票號／總帳日／編製人員／傳票總額）加上動態的 C1..CN 欄。某傳票命中某條件時，該 C 欄打 ✓，否則留空。表格下方〔載入更多〕逐頁接續。
    3. **行層明細**（step4-1）：列出命中傳票的**所有行**，包含未命中任何條件的行。有固定欄（傳票號／項次／總帳日／科目／金額／摘要）加上動態的逐行 C tag（該行命中才打 ✓）。沒有單傳票過濾，靠傳票號欄對照。表格下方〔載入更多〕逐頁接續。
  - 預期結果：
    - C 欄數等於已存情境數；欄序與情境摘要的 position 升冪一致；C 欄標頭 hover 時用 tooltip 顯示情境名稱。情境到 10 個時欄位仍正確對位、不錯位。
    - 打 ✓ 或留空是由後端的 matchedPositions 決定的：某傳票或某行在哪些 C 欄打 ✓，要與情境摘要的命中數、以及該情境單獨〔預覽〕的命中傳票或行對得起來。
    - 傳票矩陣與行層各自的〔載入更多〕逐頁接續（≤200 列／頁）；接到底時鈕消失；載入中時鈕顯示「載入中…」並 disabled。大母體逐頁載入要流暢，寬矩陣在框內水平捲動、不整頁爆版。
    - 〔收合矩陣〕後再〔展開矩陣〕不應重抓（有快取）。沒保存任何情境時，矩陣卡顯示友善的空狀態、不出現展開鈕。
  - 失敗回報格式：在哪一段（摘要／傳票矩陣／行層）、做了什麼、看到什麼；F12 console 有錯一併附上。常見問題例如：C 欄數或欄序不符、✓ 對位錯、命中數對不起來、接續列不前進、接到底鈕沒消失、寬表爆版、postMessage 例外。

（子專案 B、A、F 等已於 2026-06-19 與 06-20 實機驗收通過，見下方「已完成」段。）

---

## 已完成（僅供追溯，不必再驗）

- **WebView2 檔案對話框 reentrancy 崩潰修復 GUI 驗收通過（2026-06-24 實機）**：`host.selectFile`／`selectFiles`／`selectSavePath` 改走 `Form1.ShowDialogDeferredAsync`（`BeginInvoke` 把對話框延到 `WebMessageReceived` 返回之後再開）後，多來源多檔、單檔（行事曆/科目配對/授權清單）、匯出存檔三類對話框反覆「開→選/取消」（含快速連點）都不再以 `0x80000003` 閃退或凍結，無 postMessage 例外。權威依據 WebView2 Threading model › Reentrancy 與 WebView2Feedback #2946／#4648。
- **D1 五處明細「載入更多」GUI 驗收通過（2026-06-24 實機）**：完整性測試、借貸不平、INF 抽樣（空表起接）、空值三子項（日期區間外/空白摘要/空白傳票號·科目，後者一格兩鈕各自分頁）、已儲存情境命中，五處逐頁接續；首次點擊清掉預覽列改為單一升冪全量、接續不與預覽重複、接到底鈕消失、載入中 disabled、大母體流暢、寬表框內捲動。
- **編製人員升級（子專案 C，編製人員規則升級）前端 GUI 驗收通過（2026-06-24 實機）**：「授權編製人員清單」卡三態；預篩選「非授權編製人員」（清單未匯入＝虛線「無法執行」＋原因，匯入後＝命中/未發現）與「低頻編製者」（永遠可執行）；「進階條件篩選」自訂「編製人員張數」條件（預設 11/min 1/step 1）與摘要、預篩選鍵閘門隨授權清單匯入解鎖；以「非授權編製人員」組情境的 escalation 命中明細逐頁接續（走 D1 `filterHitsPage`）。
- **低頻科目升級（子專案 C 補遺，低頻科目規則）前端 GUI 驗收通過（2026-06-24 實機）**：預篩選「低頻科目之分錄」永遠可執行、命中〔檢視〕逐頁預覽；「進階條件篩選」自訂「科目張數」條件（預設 11/min 1/step 1）與摘要、預篩選鍵「低頻科目」永遠可選；escalation 逐頁接續；「低頻科目 AND 科目名稱 NotContains」（重現方法學 step3 C9）保存/預覽/命中數正常。
- **資料與 schema 擴充 GUI 驗收通過（2026-06-20 實機）**：子專案 B（資料／schema 擴充）。「欄位配對」的 GL 多了選填的「傳票日期」，可指派並提交，也可以不指派；「資料驗證與測試」的預篩選「回溯過帳」會命中過帳日早於傳票日的傳票，傳票日空白時不命中；「進階條件篩選」可引用「回溯過帳」row-tag 組情境並預覽；完整性測試顯示 part(a) 控制總數（來源與投影母體的列數、借貸總額，以及是否一致）；完整性差異中的 Not-in-TB 標示都如設計運作。決策脈絡見 `docs/development-log.md`。
- **規則校正 GUI 驗收通過（2026-06-20 實機）**：子專案 A（規則校正）。連續零尾數規則顯示「本次門檻：尾數連續 6 個 0」；進階條件篩選可保存到第 10 個情境（容量顯示「N / 10」），第 11 個被擋下；摘要特定描述命中時包含「帳外」；授權閘（自訂尾數位數 6 AND 金額區間 ≥ 門檻）的預覽列數合理。都如設計。決策脈絡見 `docs/development-log.md`。
- **行事曆檔案匯入 GUI 驗收通過（2026-06-20 實機）**：子專案 F（行事曆檔案匯入）。「匯入資料」日期維度卡上傳假日／補班 `.xlsx`（真實事務所檔），只收 IS_Holiday=Y、跨年度全收，假日與補班互不歸零，上傳後相關規則失效，錯誤路徑（非 .xlsx／缺欄）的文案都如設計。決策脈絡見 `docs/development-log.md`。
- **欄位配對雙介面與重新匯入回饋 GUI 驗收通過（2026-06-19 實機）**：
  - 欄位配對的雙介面切換（預設經典三欄表、session 記住選擇、切換無損）、經典三欄表的觀感（含下拉的 focus 框）、二維表右側的必填鐵軌（指派即翻綠、字面值也計入、全綠才可提交）、加寬約 1200px 的兩欄版面與寬批次捲動、兩介面的五態切換，都如設計。決策脈絡見 `docs/development-log.md`。
  - 匯入資料：重新匯入或加入來源完成後，卡片的成功態會出現並在數秒後淡回，append 文案正確，訊息面板的文字量沒有增加。決策脈絡見 `docs/development-log.md`。
- **欄位配對二維標頭下拉表 GUI 驗收通過（2026-06-19 實機）**：草稿態的二維配對表（標頭下拉指派、內嵌前 10 列、模式即時重算、指派單一性、side/flag 字面值的輸入與防護、必填把關、五態切換、寬批次版面）都如設計運作。決策脈絡見 `docs/development-log.md`。
- **GUI 全流程手動驗收通過（2026-06-18 實機）**：
  - 匯入資料：卡片三態、多來源精靈（Big5 不亂碼、`column_mismatch` 報錯）、逐來源預覽、橫幅文案（重新匯入／加入來源）、科目配對檔、CSV 與 .txt、真實大檔（約 114MB、約 140 萬列）。
  - 欄位配對：五態的收合與失效提示、〔預覽標準化資料〕。
  - 資料驗證與測試：規則的中文呈現與狀態徽章、檢視詳情（嵌套外觀、寬表在框內捲動不爆版）、空值拆成三列（日期區間外／空白摘要／空白傳票號碼·科目）、預篩選命中預覽（前 50 筆，週末與假日兩側）、編製者與較少使用科目的彙總表。
  - 進階條件篩選：情境預覽、已儲存情境的詳情預覽（前 10 筆）、各條件型別與驗證、〔預覽測試母體〕。
  - 其他：資料預覽面板（四個資料集各 ≤50 列）、步驟導航與閘門、回專案選擇、進度保存與 resume、訊息面板的未讀徽章、DEV 面板的閘控與診斷日誌 NDJSON（Debug 產檔、Release 不產）。
- 完整測試套件已多輪在 Windows 實機跑過全綠；最新一輪為 **525／525、0 略過**（本機 LocalDB 在線，SQL Server 測試實跑）。
- GUI 匯入「執行逾時」已解決：SqlBulkCopy 改造加上 `CommandTimeout` 防護後，使用者實測 140 萬列 88.7 秒完成。
- SQL Server SqlBulkCopy 的規模煙霧測試，以及真實 PBC 匯入的自動化部分，都已在開發機跑過全綠。
